

<div class="container">
  <%= form_for(@copy_right) do |f| %>
      <% if @copy_right.errors.any? %>
          <div id="error_explanation" class="alert-warning">
            <h2 align="center"><%= pluralize(@copy_right.errors.count, "error") %> prohibited this gallary from being saved:</h2>

            <ul>
              <% @copy_right.errors.full_messages.each do |message| %>
                  <li align="center"><%= message %></li>
              <% end %>
            </ul>
          </div> <br/>
      <% end %>


      <div class="form-group row">
        <label for="lgFormGroupInput" class="col-sm-2 col-form-label col-form-label-lg">Copy Right Title</label>
        <div class="col-sm-10">
          <%= f.text_field :copy_right_title, :class=>"form-control form-control-lg"%>
        </div>
      </div>

      <div class="form-group row">
        <div align="center">
          <%= f.submit :class=>"btn btn-primary" %>
        </div>
      </div>

  <% end %>
</div><br/>

